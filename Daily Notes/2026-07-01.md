# 2026-07-01 周三

## 今天做了什么

- **Lovart 引擎修复** — 诊断 Lovart 前端输入框 data-testid 变更问题，通过用户提供的 DOM 源码确认 `generator-prompt-input` 已改为 `agent-image-generator-prompt`，且元素类型从 `textarea` 变为 `div[contenteditable]`，更新了 `lovart_engine.py` 的 `input_box` 选择器
- **01画布项目 总控.ps1 大改**：
  - U 键 `git pull` 前加 `git reset --hard HEAD`，解决本地修改导致的合并冲突
  - U 键拉取更新后自动对 Nomi 执行 `pnpm install --frozen-lockfile`
  - 启动窗口从隐藏改为可见，实时显示日志输出
  - 运行时显示真实端口，停止时不显示端口
  - 状态检测支持从日志中查找 Vite 实际端口
- **Nomi 启动问题排查**：
  - 发现 `@photo-sphere-viewer/core` 依赖缺失，`pnpm install` 解决
  - 发现 Windows Defender 拖慢 Vite 文件读取（排除 `node_modules` 后解决）
  - 尝试改 `vite.config.ts` 的 `noDiscovery: false` 但效果不佳，保留现状
- **飞书文档文件名乱码修复** — 去除零宽字符恢复正常文件名
- **Lovart OpenAI 兼容代理尝试失败** — 写了一个 `lovart_proxy.py` 让 Infinite-Canvas 通过 OpenAI 协议调 Lovart API，中文聊天测试通过，但：
  - `start_lovart_proxy.ps1` 反复失败（Base64 编码损坏中文路径、闪退），用户给总控.ps1 做模板也抄不好
  - `/v1/models` 先写死模型列表被用户批评"坑人"，改真实 API 拉取后用户已失去信任
  - 最终用户要求删除所有 Lovart 代理相关代码，恢复原状
