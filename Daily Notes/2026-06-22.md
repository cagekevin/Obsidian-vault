# 2026-06-22 日志

## 今天要做
- [ ] 恢复 settings.json 到原始状态（tokendance 网关）
- [ ] 继续搞 Claude Code + 本地模型

## Claude Code + Ollama 配置进展

**最新发现：**
- `settings.json` 的 env 设 `ANTHROPIC_MODEL=qwen3.5:9b` 后直接 `claude` 能连上本地 Ollama
- 但 9B 模型跑着很卡
- `--model` 参数方式报 `模型不存在`，因为 Claude Code 内部改了模型名
- `my-qwen`（自定义 GGUF）模板解析错误还没解决

**待做：**
- 配置还没完全对
- 恢复 settings.json 到原始状态
- 看要不要换个轻量模型
