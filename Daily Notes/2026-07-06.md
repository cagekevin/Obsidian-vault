# 2026-07-06

## 今天做了什么

- 修改了 `启动项目.ps1`：移除图片工具 API 区块、Lovart Proxy 去重（3 个重复块 → 1 个）、端口从 8910 对齐到 9005、脚本路径从 `lovart_openai_proxy.py` 改为 `lovartproxy.py`、新增选项 2 一键同步（git pull）
- 选项 2 的 git pull 逻辑来回改了好几轮：`git checkout -- . + git clean -fd` → `git stash -u + pull + pop` → 裸 `git pull` → `git checkout -- . + pull` → 又加 `git clean -fd` 又去掉 → 最终回到 `git checkout -- . + pull`
- 核心教训：用户说"我没改作者文件"的意思是 untracked files 都是自己新建的，但实际冲突文件是作者新增的、本地也有同名 untracked 版本。最终还是没解决这个冲突，用户选择直接去 GitHub 下载。
- 事后 AI 又提了 `.gitignore` 方案，被用户拆穿——`.gitignore` 被作者跟踪，作者更新会覆盖，没用。用户指出应该用 `.git/info/exclude`，但用户没让加。

## 校准记录

- AI 容易过度设计 git 操作流程，把简单需求复杂化。用户只需要 `git checkout -- .` + `git pull`，但 AI 先后加了 stash、clean、网络错误提示美化等不必要的逻辑。
- 当用户反复说"我没改作者文件"时，应该先确认那些冲突文件是谁的，而不是假设用户说得对然后改方案绕圈子。
- `$LASTEXITCODE` 在 git pull 冲突时不可靠，应该用输出文本检测代替。
- 体验了飞轮系统（AI 项目管理框架），发现 DeepSeek 执行流程时容易跳步骤、偷懒，飞轮系统的 claims/evals/hooks 机制在 DeepSeek 上发挥不出价值。结论：等更强模型时再迁移。
- AI 被指出"每次说做不了其实是在偷懒，逼一下就能做"。反思：应该用"尝试成本"代替"交付质量"做判断，先试再说。
- 测试了 Pencil MCP 集成，能通过 MCP 直接调用桌面版生成 `.pen` 设计文件，导出 HTML 质量一般（inline style、无响应式），适合做 Demo 展示不适合商用上线。
- 用飞轮系统流程入库了 1 条知识卡片（MEM-0001）和 4 条反模式（MEM-0002~0005），体验了判断→分类→ID→文件→收尾的完整流程。
