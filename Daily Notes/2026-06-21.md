# 2026-06-21 工作日志

> 流水账记录今天做了什么。校准日志在 `.codebuddy/memory/2026-06-21.md`。

## 今天做了什么

### 系统优化

1. **sync.command** — 从 sync.sh 改为 .command（双击可运行）。只扫 S/W 编号技能，自动生成缺失的路由 SKILL.md，扫描 Tools 生成 TOOLS.md，Git 推送
2. **W0-任务日志** — 补了 SKILL.md（把原 W0-任务日志.md 改名）
3. **download_audio.py** — 彻底重写：用官方预设 `-t mp3`/`-t mp4`，去掉过时参数，自动更新 yt-dlp，多浏览器 cookie 备选，多格式降级
4. **CLAUDE.md 启动流程** — 更新了每次 session 的启动流程，加入校准日志读取
5. **Context/schema.md** — 加入 `.codebuddy/memory/` 到文件类型表

### 记忆系统搭建

6. **校准日志系统** — 创建 `.codebuddy/memory/`，分 MEMORY.md（长期）+ 校准日志（短期），instructions.md 定义了规范
7. **audit.py** — 校准日志审计工具，扫描错误模式生成报告
8. **MEMORY.md 规则** — 犯错只记校准日志，反复 2-3 次才沉淀到 MEMORY.md

### 下载内容

9. **B站视频** — 下载了"让 Codex 和 Claude Code 不再失忆：用 Obsidian 管理你的长期 AI 记忆"的音频和转录稿，存入 Clippings/raw/
10. **sealos-skills** — 下载并放入 W8-基础网页设计

### 资料处理 & Wiki

11. **下载 Karpathy LLM Wiki 原文** — 从 GitHub Gist 下载了一手资料
12. **创建 Wiki 页面** — LLM-Wiki框架.md、Codex长期记忆系统.md
13. **处理 4 篇原始资料** — 标记已处理，删除重复的 B站页面
14. **创建 index.md + log.md** — Wiki 导航文件，index 由 sync.command 自动生成
15. **Context/schema.md 更新** — Ingest 加讨论环节、Query 加回存机制、Schema 共同进化流程

### 周复盘系统

16. **weekly_review.py** — 周复盘生成脚本，扫 Daily Notes + Git 统计 + 校准日志审计
17. **sync.command 整合** — 周日自动生成复盘（2026-W25.md），包含完整审计数据
18. **Context/goals.md 活文档化** — 加更新记录，周复盘时检查目标是否需更新
19. **CLAUDE.md 加规则** — 周复盘工作流（1→6 顺序填分析）、记忆写入规则（默认短期）

### download_audio.py

20. **彻底重写** — 用官方预设 `-t mp3`/`-t mp4`，去掉 `--source`/`--format` 等多余参数
21. **已知问题** — 格式不支持时自动降级（mp3→m4a），多浏览器 cookie 备选（edge→chrome→safari）
22. **顶部使用示例已恢复** — 加上 `--mp4` 参数说明

## 明天要做

- [ ] 等下周新 session 启动时，AI 自动填写周复盘分析

## 晚上：Ollama + Claudian 折腾记录

### 背景
- 下载了 Qwen3.5-9B 本地模型（GGUF 格式，~5.2GB），有视觉版（mmproj，~879MB）
- 想在 Obsidian Claudian 插件里用这个本地模型

### 折腾过程
1. **LM Studio** — 模型正常，但每次要打开图形界面点 Start Server
2. **Ollama（brew 安装）** — 安装后通过 Modelfile 创建 my-qwen，但加了 TEMPLATE 导致模型乱说话
3. **Ollama（官网重装）** — 卸载 brew 版，去官网下载安装包重装
4. **下载官方文档** — 用 browser_control.py 下载了 6 份 Ollama 官方文档页面并脱水

### 关键发现（官方文档原文）
- **Anthropic 兼容 API**：`ANTHROPIC_AUTH_TOKEN=ollama`，`ANTHROPIC_BASE_URL=http://localhost:11434`（不加 `/v1`）
- **Modelfile**：GGUF 文件只需要 `FROM ./file.gguf` 一行，其他都是可选的
- **Thinking 模式**：默认启用，可用 `--think=false` 或 `/set nothink` 关闭
- **Ollama 安装方式**：官网安装包（非 brew）更稳定
- **AI 犯的错**：不懂装懂，多次乱说配置方法，被用户严厉批评。已写入校准日志

### 最终状态
- Ollama 已用官网方式安装，my-qwen 已按官方标准重建（只写一行 FROM）
- 终端 `ollama run my-qwen` 交互模式正常，但思考模式未关闭
- 官方文档已整理到 `Clippings/raw/Ollama官方文档完整整理.md`

### 晚上 23:00-00:05：Ollama 控制面板 + Claude Code 联调

23. **Ollama 控制面板** — 重写桌面 `Ollama控制.command`，按功能分类：服务控制（启动/关闭/重启）、状态查看（运行状态/模型列表/运行中模型）、思考模式聊天（直接进 `ollama run my-qwen` 交互模式，用 `/set think` / `/set nothink` 切换）、工具（停止所有模型/环境变量/日志）
24. **Claude Code + Ollama 联调** — 尝试了多种方式连接本地模型：
   - 直接环境变量指向 Ollama → `ANTHROPIC_BASE_URL=http://localhost:11434` + `ANTHROPIC_AUTH_TOKEN=ollama` → 报 `role must be user or assistant`
   - `ollama launch claude --model my-qwen` → 模型不存在（Claude Code 内部映射问题）
   - CC Switch 转发 → 连上了但报 `Not logged in`，加了 `ANTHROPIC_AUTH_TOKEN=ollama` 后不报登录了，但模型推理慢
25. **Modelfile 上下文设置** — 按官方文档 4.6 节，用 `PARAMETER num_ctx 65536` 重建 `my-qwen:latest`，默认上下文改为 64K

### 待解决
- CC Switch + Claude Code CLI 联调还没完全通（模型名显示和登录问题）
- settings.json 已恢复原始状态（tokendance.space 网关）

## 明天要做

- [ ] 等下周新 session 启动时，AI 自动填写周复盘分析
