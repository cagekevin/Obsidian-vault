## 2026-06-20 工作记录

### Obsidian 同步方案重构

- 将 `.gitignore` 中 `.obsidian/` 的完全忽略改为只忽略 `workspace.json`、`workspace-cache/`、`cache/`
- 同步了 .obsidian/ 配置到 GitHub（含插件列表、设置）
- 修改 `sync.sh`：只保留技能清单扫描功能，去掉 git commit/push 操作
- 更新 README 同步指令说明
- 推荐方案：GitHub + Obsidian Git 插件自动同步，零成本，版本控制天然适合文本 vault
